{{ template "header" . }}

const (
{{- range $key, $value := .CodeGen.CodeGeneratorRecords }}
	{{- $metadata := $value.CodeGeneratorMetadata }}
	{{ $metadata.ConstName }} {{ $.CodeGen.CodeGeneratorMetadata.DataType }} = {{ $metadata.ConstValue }} // {{ if $metadata.IsDeprecated }}Deprecated: {{ end }}{{ $metadata.ConstComments }}
{{- end }}
)

{{ template "static_data" . }}