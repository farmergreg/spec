{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "specgen",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/src/cmd/specgen",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "GOPATH": "${env:GOPATH}",
                "GOROOT": "${env:GOROOT}"
            }
        }
    ]
}